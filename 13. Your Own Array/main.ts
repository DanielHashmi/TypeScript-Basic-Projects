let favTranspMode: string[] = ['Honda MotorCycle', 'Honda Civic', 'BMW', 'Helicopter'];

for (let i:number = 0; i < favTranspMode.length; i++) {
    console.log(`“I would like to own a ${favTranspMode[i]}".`)
}